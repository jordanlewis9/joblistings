import React from 'react';
import ListingsContainer from './ListingsContainer';

const App = () => {
    return (
        <div>
            This is the app component
            <ListingsContainer />
        </div>
    );
}

export default App;